export enum RoutesAPI {
  login = "/user/login",
  register = "/user/register",
  requestReset = "/reset/request",
  resetPWD = "/reset/",
  about = "/",
}

export enum Routes {
  home = "/",
  login = "/login",
  register = "/register",
}
